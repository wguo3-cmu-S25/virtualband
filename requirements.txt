# Audio to Base64 Converter Requirements
# This script uses only Python built-in libraries, so no external dependencies are required.
# Python 3.6+ is recommended for optimal compatibility.

# Optional: If you want to add audio processing capabilities in the future
# pydub==0.25.1
# librosa==0.10.1 